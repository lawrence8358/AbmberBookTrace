## Agent skills

### Issue tracker

問題會用 Markdown 檔案放在 `.scratch/<feature>/`。詳情請看 `docs/agents/issue-tracker.md`。

### Triage labels

使用五個固定的英文問題分類標籤。詳情請看 `docs/agents/triage-labels.md`。

### Domain docs

使用單一背景的文件配置，包含根目錄的 `CONTEXT.md` 和 `docs/adr/`。詳情請看 `docs/agents/domain.md`。
